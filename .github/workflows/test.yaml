name: CI
on:
  push:
    branches: [ roni ]
jobs:
  run-robotframework-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Robot Framework
        uses: joonvena/robotframework-docker-action@v1.0
        with:
          robot_tests_dir: ./
      - run: ls -la
